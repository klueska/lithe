To build the hard threads library execute:

  make prefix=/path/to/directory all

This will build the shared library. To install the shared library execute:

  make prefix=/path/to/directory install

Likewise, to uninstall do:

  make prefix=/path/to/directory uninstall
